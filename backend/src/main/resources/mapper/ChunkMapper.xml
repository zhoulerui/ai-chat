<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.aichat.mapper.ChunkMapper">

    <select id="selectAllChunks" resultType="com.example.aichat.entity.Chunk">
        SELECT id, game_id, article_id, content
        FROM chunk
    </select>

    <select id="selectChunksByGame" resultType="com.example.aichat.entity.Chunk">
        SELECT id, game_id, article_id, content
        FROM chunk
        WHERE game_id = #{gameId}
        ORDER BY id DESC
        LIMIT 200
    </select>

    <insert id="insertChunk" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chunk(game_id, article_id, content, embedding, created_at)
        VALUES(#{gameId}, #{articleId}, #{content}, #{embedding}, NOW())
    </insert>

    <delete id="deleteChunk">
        DELETE FROM chunk WHERE id = #{id}
    </delete>

    <delete id="deleteChunksByArticle">
        DELETE FROM chunk WHERE article_id = #{articleId}
    </delete>

    <!-- 用下划线别名 + resultType Bean:MyBatis 驼峰映射(map-underscore-to-camel-case),
         避免 H2/MySQL 对别名大小写的差异 -->
    <select id="selectArticleByChunkIds" resultType="com.example.aichat.dto.ArticleBrief">
        SELECT c.id AS chunk_id, a.id AS article_id, a.title AS title, a.content AS content
        FROM chunk c
        LEFT JOIN article a ON c.article_id = a.id
        WHERE c.id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

</mapper>
