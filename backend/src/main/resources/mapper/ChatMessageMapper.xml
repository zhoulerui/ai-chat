<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.aichat.mapper.ChatMessageMapper">

    <select id="selectByConversation" resultType="com.example.aichat.entity.ChatMessage">
        SELECT id, conversation_id, role, content, references_json, created_at
        FROM chat_message
        WHERE conversation_id = #{conversationId}
        ORDER BY id ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_message(conversation_id, role, content, references_json, created_at)
        VALUES(#{conversationId}, #{role}, #{content}, #{referencesJson}, NOW())
    </insert>

</mapper>
